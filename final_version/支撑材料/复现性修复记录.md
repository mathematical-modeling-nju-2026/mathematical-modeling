# 支撑材料复现性修复记录

日期：2026-09-13
起因：按附录 B 的复现方法、在**隔离环境**（清空全部预存结果）验证支撑材料能否从零复现。

验证方式：把 `final_version/支撑材料/` 复制到 `d:\_repro_test`，清空全部 `*/results/`
（原结果备份至 `d:\_repro_backup`），严格按附录 B 命令执行。

---

## 一、发现的问题

### 问题 1（致命）：附录 B 缺步骤，Q2 流程在第 2 步即中断

附录 B 原文只写两步：

```
python Q2/code/run_experiment.py --first-residual 7 --only uniform56 --out recommended_only
python Q2/code/make_target_tables.py
```

实测报错：

```
FileNotFoundError: '...\Q2\results\schedule_detail.csv.gz'
```

原因：`--out recommended_only` 把结果写入 `Q2/results/recommended_only/`，
而 `make_target_tables.py` 读 `Q2/results/schedule_detail.csv.gz`。
**漏掉了 `report_experiment.py`** —— 它的职责正是把推荐方案从子目录提升到
`Q2/results/` 根（`shutil.copy2`）。且完整流程需要 3 个实验口径分组，
附录只写了 1 个。

### 问题 2：`verify_window.py` 表头断言与结果文件不符（Q2 核验必然 FAIL）

```
errors: ['plan_interval_headers', 'plan_slot_energy_max_error']
plan_slot_energy_max_error = 1455.73
```

`verify_window.py` 第 304 行硬编码期望表头 `0:00-0:10, 0:10-0:20, ...`，
但 `result2.xlsx` 用的是**附件5 模板的环形表头**：

| | 第 1 格 | 最后 1 格 |
|---|---|---|
| 实际 | `0:10-0:20` | `0:00-0:10+1` |
| 期望 | `0:00-0:10` | `23:50-24:00` |

表头不匹配 → 整列**错位**比较 → 单格误差高达 1455.73 kWh。
（与 2026-09 修复的 Q3 `verify_q3.py` 属同一类缺陷，当时 Q2 漏修。）

**附加发现**：官方模板个别标签分钟位不足两位，如 `7:0-7:10`（应为 `7:00-7:10`），
解析时须按整数读取分，不能用 `\d{2}` 强约束。

### 问题 3：支撑材料缺 `common/q2_base/summary.json`

`report_experiment.py` 第 40 行读 `SOURCE.parent/'summary.json'`，
用于校验"原目录基线已精确复现"（`old_gap <= 1e-6`），但该文件未收录。

### 问题 4：`Q4/part3/code/run_q3.py` 缺 `import sys`

第 21 行使用 `sys.path` 但未导入：

```
NameError: name 'sys' is not defined. Did you forget to import 'sys'?
```

（与 2026-09 修复的 Q3 `run_q3.py` 属同一 bug，Q4-3 这份漏修。）

### 问题 5（非缺陷）：`--out` 会破坏后续核验

`run_q3.py` / `run_q42.py` 的 `--out` 默认值为 `HERE`（本问 `results/` 根），
正确用法是**不传** `--out`。附录 B 原文传了 `--out reproduced` / `--out out`，
导致结果写入子目录、核验脚本找不到。

---

## 二、修改内容

| 文件 | 修改 |
|---|---|
| `Q2/code/verify_window.py` | 新增 `_interval_slot_order()`：按标签解析表头得到真实 slot 顺序（含环形回绕、兼容 `7:0` 式不足两位分钟）；`check_workbook()` 改为按该顺序取值比对，替换原硬编码 `expected_labels` 断言 |
| `Q4/part3/code/run_q3.py` | 补 `import sys` |
| `common/q2_base/summary.json` | 新增（原目录基线，供发布前复核） |
| `common/q2_base/verification.json` | 新增（同目录配套核验记录） |
| `paper/sections/09_appendix.tex` | 附录 B 复现步骤改为**完整且实测通过**的 18 步流程；补充 5 条注意事项（含"不要传 `--out`"）；附录 A 表格补注 `summary.json` |

修改同时应用于仓库副本，保持两处一致：

- `question2/code/verify_window.py` ≡ `final_version/支撑材料/Q2/code/verify_window.py`
- `question4/part3/code/run_q3.py` ≡ `final_version/支撑材料/Q4/part3/code/run_q3.py`

---

## 三、修复后全链路实测结果

在清空结果的隔离环境中，按新附录 B 流程逐步执行：

| 步骤 | 命令 | 退出码 | 关键数值 |
|---|---|---|---|
| Q1 | `Q1/q1_code/run_q1.py` | 0 | 购电费 **35126.95 元** |
| Q2-1 | `Q2/code/run_experiment.py` | 0 | 基线 1440.20 万元 |
| Q2-2 | `run_experiment.py --extended ... --out extensions/short_windows` | 0 | — |
| Q2-3 | `run_experiment.py --first-residual 7 --initial 8550 ... --out sensitivity/valid_residuals` | 0 | 推荐 1438.914 万元 |
| Q2-4~6 | `verify_window.py`（主 / short_windows / valid_residuals） | 0 | 11 变体 **全部 PASS** |
| Q2-7 | `check_information.py` | 0 | 通过 |
| Q2-8 | `report_experiment.py` | 0 | 发布到 results/ 根 |
| Q2-9 | `make_target_tables.py` | 0 | 表1/2/3 = 28/32/49 行 |
| Q3 | `Q3/code/run_q3.py --mode all` | 0 | **1402.24 万元** |
| Q3-V | `Q3/code/verify_q3.py` | 0 | 通过 |
| Q4-2 | `Q4/part2/code/run_q42.py --primary joint_price` | 0 | **1516.30 万元** |
| Q4-2-V | `verify_q42.py` | 0 | 4 方案 **全部 PASS** |
| Q4-2-I | `check_information.py` | 0 | 通过 |
| Q4-2-R | `report_q42.py` | 0 | 发布到 results/ 根 |
| Q4-3 | `Q4/part3/code/run_q3.py --mode all` | 0 | **1506.17 万元** |
| Q4-3-V | `Q4/part3/code/verify_q3.py` | 0 | 通过 |

**18 步全部 exit=0。**

复现结果与原提交结果比对（费用指标）：

| 问题 | 原结果（元） | 复现结果（元） | 一致 |
|---|---|---|---|
| Q2 | 14,389,135.08 | 14,389,135.08 | ✓ |
| Q3 | 14,022,396.98 | 14,022,396.98 | ✓ |
| Q4-2 | 15,163,043.89 | 15,163,043.89 | ✓ |
| Q4-3 | 15,061,735.43 | 15,061,735.43 | ✓ |

Q1 最优解 `solution.npz` 的 g/c/d/w/E 逐元素 `max|diff| = 0`。
（xlsx/csv 哈希有差异，源于浮点序列化与时间戳写回，非数值差异。）
