# 公共依赖

`q2_base/run_1439.py` 来自原方案B基础目录，`q2_data.py` 来自原 `C_yang/q2`，是第二问及4-2复用的预测、LP、读取层。其同目录结果是原始对照基准，保留供 `question2/code/report_experiment.py` 比较；不是最终推荐结果。原 `OPT_V3` 变量未被使用，移除其过时外部路径声明，预测器始终在本文件内部实现。

`plotting/` 来自仓库中已有的样式与导出实现，第一问绘图和4-2报表使用它，不再读取作者个人技能目录。旧方案中的副本仍作为历史证据保留，没有为整理重构算法或合并第三问与4-3不同的价格模型。
