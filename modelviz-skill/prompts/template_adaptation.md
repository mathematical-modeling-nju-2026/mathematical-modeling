# Template Adaptation

Use the selected template source code as the base. Preserve the core layout, style, color logic, legend and axis treatment where possible. Replace simulated data with the user's real data columns from `adaptation_plan.json`. Do not modify files under `templates/` and do not overwrite user data.
