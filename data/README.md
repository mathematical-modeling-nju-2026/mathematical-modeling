# 共享原始输入

从原 `C_yang/raw` 移入，附件1—4、附件5模板和C题PDF不改字节。第一问用附件1，第二问用附件1、2；第三问加附件3，4-2加附件4，4-3使用附件1—4。附件5是原始空模板，不与各问 `results/` 的结果表混用。

原始Excel临时文件 `.~附件1.xlsx` 移入 `archive/temporary/`。4-3迁移前读取本地 `CUMCM2026Problems/C题/附件`，迁移检查会核对两份附件是否一致；新默认输入为本目录。环境变量 `CUMCM_C_ATTACHMENT_DIR` 可继续显式覆盖4-3附件位置。
