# 效率口径说明（问题 4）

## 现状

鉴于问题 1 已完成两种「90% 效率」口径的对照实现并给出定量差异
（见 `question1/efficiency/`），本目录为问题 4 预留分类骨架，**当前尚未实现**。

## 两种口径（与问题 1 一致）

| | 定义一（储能侧口径） | 定义二（国标往返口径） |
|---|---|---|
| 递推式 | $E_t=E_{t-1}+0.9c_t-d_t/0.9$ | $E_t=E_{t-1}+\eta c_t-d_t/\eta$，$\eta=\sqrt{0.9}$ |
| 单向效率 | $\eta_c=\eta_d=0.9$ | $\eta_c=\eta_d=0.948683$ |
| 往返效率 | **0.81** | **0.90** |
| 对应结果 | 现有 `part2/results/`（1516.30 万）、`part3/results/`（1506.17 万） | 待生成 |

## 待改造的具体位置

问题 4 分两部分，代码分别位于 `question4/part2/code/` 与 `question4/part3/code/`：

### 4-2（`part2`）

| 文件 | 说明 |
|---|---|
| `part2/code/q42_model.py` | 复用 `common/q2_base` 的模型层，效率不在本文件硬编码 |
| `common/q2_base/run_1439.py` | 第 46 行 `ETA_C = ETA_D = 0.90` 为**根源**；第 145、180 行使用 |

→ 改 4-2 的效率须同时改公共基础层，**注意会连带影响问题 2**，
建议通过参数注入而非直接改常量。

### 4-3（`part3`）

与问题 3 完全同构（是问题 3 代码的副本 + 实时电价结算）：

| 行号（`part3/code/q3_model.py`） | 现有实现 | 改造要点 |
|---|---|---|
| 12 | `ETA = 0.9` | 拆为 `ETA_C` / `ETA_D` |
| 154 | 节点 SOC 递推 | 充电项 `-ETA_C`，放电项 `1/ETA_D` |
| 176 | LP 递推 | 同上 |
| 185–186 | 互斥清理 `ETA**2` | 改为 `ETA_C * ETA_D` |

## ⚠️ 连带影响提示

`common/q2_base/run_1439.py` 被**问题 2 与 4-2 共用**。
若直接修改其中的 `ETA_C`/`ETA_D`，会同时改变问题 2 的结果。
推荐的落地方式：

1. 为该脚本增加 `--eta-c` / `--eta-d` 命令行参数（默认 0.90，保持向后兼容）；
2. 结果按口径写入子目录，不覆盖现有 `results/`。

## 建议的目录结构

```
question4/efficiency/
├── def1_side90/
│   ├── part2/results/     定义一 4-2（1516.30 万）
│   └── part3/results/     定义一 4-3（1506.17 万）
└── def2_roundtrip90/
    ├── part2/results/     定义二 4-2
    └── part3/results/     定义二 4-3
```

## 参考

问题 1 的完整实现与对照结论见 `question1/efficiency/README.md`。
