% ==================== 附录 ====================
\appendix
\renewcommand{\thesection}{附录 \Alph{section}}
\section{支撑材料文件列表}

本次提交包含两部分：论文正文（\texttt{论文.pdf}）与支撑材料文件夹
\texttt{final\_version/支撑材料/}（压缩为单个文件提交）。
下表给出支撑材料的目录结构与内容说明。
所有源程序均为完整、可直接运行版本，运行环境见 \S\ref{sec:appendix-env}。

\begin{table}[H]
\centering
\footnotesize
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.15}
\caption{支撑材料目录结构（\texttt{final\_version/支撑材料/}）}
\label{tab:appendix-files}
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}p{3.55cm} X c@{}}
\toprule
路径 & 内容 & 问题 \\
\midrule
\multicolumn{3}{@{}l}{\textbf{一、源程序}} \\
\midrule
\texttt{Q1/q1\_code/} & \texttt{q1\_model}（LP/MILP 建模与核验）、\texttt{run\_q1}（主流程与结果表生成） & 一 \\
\midrule
\texttt{Q2/code/} & \texttt{rolling\_window}（残差与窗口）、\texttt{run\_experiment}（滚动执行与结果表）、\texttt{verify\_window}、\texttt{check\_information}（核验）、\texttt{report\_experiment}（汇总与说明生成）、\texttt{make\_target\_tables}（题目要求表） & 二 \\
\midrule
\texttt{Q3/code/} & \texttt{q3\_data}（校验）、\texttt{q3\_forecast}（预报融合）、\texttt{q3\_model}（场景树 LP）、\texttt{run\_q3}（滚动执行与结果表）、\texttt{verify\_q3}（核验） & 三 \\
\midrule
\texttt{Q4/part2/code/} & \texttt{q42\_model}（电价预测与联合场景）、\texttt{run\_q42}（主流程与结果表）、\texttt{report\_q42}（汇总与说明、指定日表生成）、\texttt{verify\_q42}、\texttt{check\_information} & 四-2 \\
\midrule
\texttt{Q4/part3/code/} & 问题三代码的实时电价扩展（含 \texttt{requirements.txt} 依赖清单） & 四-3 \\
\midrule
\texttt{common/} & \texttt{q2\_base/}（基础 LP 与数据层）、\texttt{efficiency/}（附件5 模板表头，主程序读模板所必需） & 全部 \\
\midrule
\multicolumn{3}{@{}l}{\textbf{二、结果与图表}} \\
\midrule
\texttt{Q1..Q4/\(\ast\)/results/} & \texttt{result\(\ast\).xlsx} 正式结果、\texttt{summary.json} 汇总、\texttt{schedule\_detail.csv} 逐时段明细、\texttt{verification.json} 核验记录、\texttt{figures/} 插图源文件 & 全部 \\
\texttt{Q2/results/} 子目录 & \texttt{variants/}、\texttt{extensions/}、\texttt{sensitivity/}：各候选窗口、短窗口扩展与敏感性分析对照 & 二 \\
\texttt{Q3/results/} \texttt{comparisons/} & 四种发布时点方案（\texttt{B\_aligned}、\texttt{only\_0}、\texttt{at\_0\_6}、\texttt{at\_0\_6\_12}）结果 & 三 \\
\texttt{Q4/.../results/} & 各电价场景方案与指定日结果 & 四 \\
\midrule
\multicolumn{3}{@{}l}{\textbf{三、AI 使用说明}} \\
\midrule
\texttt{AI使用说明/} & \texttt{AI工具使用详情.pdf}（7 页，所用工具与模型、使用目的与环节、典型交互示例）及同名 Markdown 源文件 & — \\
\midrule
\multicolumn{3}{@{}l}{\textbf{四、数据}} \\
\midrule
\texttt{data/附件/} & \texttt{附件1--4.xlsx} 原始数据；\texttt{附件5/} 结果文件模板 & 全部 \\
\bottomrule
\end{tabularx}
\end{table}

\textbf{说明}：\texttt{final\_version/支撑材料/} 为自包含目录，解压后无需任何
外部文件即可按 \S\ref{sec:appendix-env} 的命令复现全部结果；
附录 \S\ref{sec:appendix-source} 的源程序\textbf{直接取自该目录}，
与所提交文件逐字一致。

\section{运行环境与复现方法}
\label{sec:appendix-env}

\begin{table}[H]
\centering
\small
\caption{软件环境}
\begin{tabularx}{\textwidth}{@{}l X@{}}
\toprule
项目 & 版本 / 说明 \\
\midrule
操作系统 & Windows \\
Python   & 3.12 / 3.13 \\
数值计算 & \texttt{numpy}、\texttt{scipy}（HiGHS 求解器）\\
数据处理 & \texttt{pandas}、\texttt{openpyxl}\\
绘图     & \texttt{matplotlib} \\
论文排版 & XeLaTeX $+$ \texttt{ctex}（中文支持）\\
\bottomrule
\end{tabularx}
\end{table}

\textbf{复现步骤}（在 \texttt{final\_version/支撑材料/} 目录下执行）：

\begin{verbatim}
python -m pip install -r Q4/part3/requirements.txt

# 问题一
python -X utf8 Q1/q1_code/run_q1.py

# 问题二（推荐方案：有效残差 + 56 天等权）
python -X utf8 Q2/code/run_experiment.py \
       --first-residual 7 --only uniform56 --out recommended_only
python -X utf8 Q2/code/make_target_tables.py     # 题目要求的表1/表2/表3

# 问题三（四次更新主方案）
python -X utf8 Q3/code/run_q3.py --mode all --out reproduced
python -X utf8 Q3/code/verify_q3.py              # 独立核验

# 问题四
python -X utf8 Q4/part2/code/run_q42.py --primary joint_price
python -X utf8 Q4/part3/code/run_q3.py --mode all --out out
python -X utf8 Q4/part3/code/verify_q3.py        # 独立核验
\end{verbatim}

\textbf{说明}：
\begin{itemize}[itemsep=2pt,topsep=3pt]
  \item 各脚本内部路径均相对于文件位置，从任意工作目录用脚本绝对路径启动均可。
  \item 问题二\textbf{无参数默认运行的是原始残差口径候选}，
        不是最终推荐方案，须按上述命令显式指定 \texttt{--first-residual 7}。
  \item 问题四-2 会读取 \texttt{Q2/results/} 下的 Q2 结果作为同口径基准，
        因此需先运行问题二。
  \item 运行主程序会覆盖同名结果文件；论文使用的现有结果应先备份。
\end{itemize}

\section{核验记录摘要}

\begin{table}[H]
\centering
\caption{逐时段核验项目（四问合计 $48\,096$ 段/年）}
\small
\begin{tabular}{@{}l l c@{}}
\toprule
核验项 & 判据 & 结果 \\
\midrule
能量平衡            & $g+V+d=L+c+w$ 残差 $<10^{-9}$ & 通过 \\
效率递推            & $E_t-E_{t-1}-0.9c_t+d_t/0.9=0$ & 通过 \\
SOC 边界            & $1200\le E_t\le10800$ & 通过 \\
充放电功率          & $0\le c_t,d_t\le 833.33\kw$ & 通过 \\
充放电互斥          & $c_t\,d_t=0$（逐段检查）& 通过 \\
跨日连续性          & 相邻日末初电量一致（偏差 $<10^{-9}$）& 通过 \\
初末条件            & 起始/终止电量符合设定 & 通过 \\
实际缺口            & $e_t=\max(N_t-z_t,0)$ & 通过 \\
费用复算            & 独立重算与程序输出一致 & 通过 \\
Excel 反读          & 读回结果文件与内部数组一致 & 通过 \\
非前视（信息扰动）  & 扰动未来数据不改变当前决策 & 通过 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{信息扰动测试的具体做法}（以问题三为例）：
在 2 月 1 日、6 月 21 日、12 月 21 日的四个发布时刻，
\textbf{修改当天及未来的全部实际值与未发布预报}，重建预测、残差、融合权重、场景树与 LP，
检查\textbf{当前的预测结果与控制量是否保持不变}。结果显示决策完全不变，
证明程序未使用任何未来信息。

\input{sections/10_required_tables}

\input{sections/09a_source_code}
